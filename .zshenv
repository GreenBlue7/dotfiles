# ~/.zshenv — Loaded FIRST by every zsh instance (login, interactive, script)
# Keep this minimal. Only universal PATH entries that must exist everywhere.

# uv (Python package manager)
export PATH="$HOME/.local/bin:$PATH"
